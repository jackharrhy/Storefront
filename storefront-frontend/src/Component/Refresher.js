import React from 'react'

export default ({onClick}) => {
  return null;
  // TODO refresh button
  return (
    <div
      id="refresh"
      onClick={onClick}
    >
      <p>Refresh</p>
    </div>
  )
}
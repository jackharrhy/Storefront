# Shared storefront code

`@storefront/shared` contains the item/storefront types, API response validation, and URL options used by the frontend and Discord screenshots. It has no runtime dependencies.

The root install and build commands compile it first. After editing it during development, run `npm run build -w @storefront/shared` to update the compiled exports.
